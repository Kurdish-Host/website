import type React from "react"
import "./globals.css"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Kurdish Host - Free Discord Bot Hosting",
  description:
    "Free Discord Bot Hosting with NodeJS, Python and more. Host your Discord bots for free with reliable performance.",
  keywords: "Discord Bot Hosting, Discord Bot, Free Host, Free host Nodejs, Free host all lang, Host discord bot",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="dark" enableSystem>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}



import './globals.css'